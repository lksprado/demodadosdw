
version: 2

models:
  - name: stg_parlamento__deputados
    description: "Modelo bronze que carrega e padroniza os dados brutos da API da Câmara"
    columns:
      - name: id
        description: "Identificador único do deputado"
        tests:
          - not_null
          - unique
